info:
  name: loginUser
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:3000/api/v1/users/login
  body:
    type: json
    data: |-
      {
        "email":"user2@gmail.com",
        "password":"pipi9996"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
