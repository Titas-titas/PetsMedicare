info:
  name: getAllMyAppointments
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:3000/api/v1/appointments/me?sort=owner_name&order=DESC
  params:
    - name: sort
      value: owner_name
      type: query
    - name: order
      value: DESC
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
