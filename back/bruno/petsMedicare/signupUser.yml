info:
  name: signupUser
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:3000/api/v1/users/signup
  body:
    type: json
    data: |-
      {
        "name":"username1111",
        "email":"user2@gmail.com",
        "password":"pipi9996"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
