info:
  name: signupUser
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:3000/api/v1/users/signup
  body:
    type: json
    data: |-
      {
        "name":"username111",
        "email":"user@gmail.com",
        "password":"pipi9996"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
