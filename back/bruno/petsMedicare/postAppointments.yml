info:
  name: postAppointments
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/api/v1/appointments
  body:
    type: json
    data: |+
      {
        "pet_name": "Diana",
        "owner_name": "M Light",
        "appointment_date": "2026-03-11",
        "appointment_time": "20:00",
        "notes": "My Dog need for the emargency"
      }

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
